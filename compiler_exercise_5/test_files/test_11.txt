program

n : 5;
globalS = "ciaociaociao" : string;

def globalS(n : int; m : int)
{
    "called globalS" !>>;
}

def passAString(s : string; ref sref : string ) : string
{
    s := "Stringa da non cambiare cambiata";
    sref := "Stringa cambiata";

    return s;
}

begin

input : string;
s = "hello hello" : string;

sToPass = "Hello" | sToPassRef = "HelloRef" : string;

input <<;

globalS := "C______C";
s := globalS + 5 + (10 * 5);

globalS(3, 4);

globalS, "  ", input !>>;
s !>>;

passAString(sToPass, sToPassRef);
sToPass, "\n", sToPassRef !>>;

end
program

a | b | c | d: int;
e | f | g : double;
h | i | l : bool;
m | n | o : string;



def fun1(a: int; ref x : double) : int{
	
	return 0;
}

def fun2(a: int; x : double){

}


begin

a := a * e + c + f;			                // intero = double
e := a * e + c - f / b;

d = a + b + n;				                // stringa = intero
m = a * b + n;
m = n + e + a;

a = fun2(3, e) + b;			                // fun2() è una procedura
a = fun1(3, e) + b;


a = h and i or l and not(a <> b);	        // intero = bool
h = (e > fun1(a, e)) or fun2();		        // fun2() è una procedura
h = fun1(3, e) <= fun1(1, e);
 




end